#include<stdio.h>
int main()
{
	int a;
	//printf("\nthe value of a is:");
	if(a>0)
	{
		printf("the number %d is positive",a);
	}
	else if(a==0)
	{
		printf("the number %d is zero",a);
	}
	else
	{
		printf("the number %d is negative",a);
	}
	return 0;
}

